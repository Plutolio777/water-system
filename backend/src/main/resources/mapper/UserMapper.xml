<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qingyuan.water.mapper.UserMapper">
    <select id="findByUsername" resultType="User">
        SELECT id, username, password, real_name, phone, role, status, created_at
        FROM sys_user WHERE username = #{username} AND status = 1 LIMIT 1
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user(username, password, real_name, phone, role, status)
        VALUES(#{username}, #{password}, #{realName}, #{phone}, #{role}, 1)
    </insert>
</mapper>
